package classwork.notes;

public class Notes {
    /*
    Lambda and Streams

    in stream we use like: value.stream
                        .filter()
                        .map().....

    Another one we use is called lambda.
     */
}
